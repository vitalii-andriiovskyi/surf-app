import { SurfShellNoRenderDirective } from './surf-shell-no-render.directive';

// there's no sense to test this directive because it works just on the server
// #TODO: find the way to test this kind of directives

// describe('SurfShellNoRenderDirective', () => {
//   it('should create an instance', () => {
//     const directive = new SurfShellNoRenderDirective();
//     expect(directive).toBeTruthy();
//   });
// });
