import { SurfShellRenderDirective } from './surf-shell-render.directive';

describe('SurfShellRenderDirective', () => {
  it('should create an instance', () => {
    const directive = new SurfShellRenderDirective();
    expect(directive).toBeTruthy();
  });
});
